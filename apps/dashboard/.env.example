# Dashboard (apps/dashboard) — server-side only credentials.
BOTORE_ENV=development
DASHBOARD_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres
# Dev-only shared secret for the login form (fails closed outside development).
DASHBOARD_AUTH_TOKEN=local-dev-token
API_HEALTH_URL=http://127.0.0.1:8080
WORKER_HEALTH_URL=http://127.0.0.1:8081
