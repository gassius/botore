# API (apps/api) — safe local placeholders. Never commit real credentials.
BOTORE_ENV=development
PORT=8080
# Local Supabase Postgres (server credential; bypasses RLS by design).
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres
SERVICE_VERSION=0.1.0-local
