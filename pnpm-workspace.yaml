packages:
  - 'apps/*'
  - 'packages/*'

# pnpm 10+ blocks dependency build scripts by default.
# This is the explicit allowlist (pnpm >= 11 schema: allowBuilds.<name>: true).
allowBuilds:
  '@next/swc-linux-x64-gnu': set this to true or false
  esbuild: true
  sharp: true
  unrs-resolver: true
