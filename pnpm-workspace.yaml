packages:
  - "apps/*"
  - "packages/*"

# pnpm 10+ blocks dependency build scripts by default. This is the explicit
# allowlist (pnpm >= 11 schema: allowBuilds.<name>: true).
allowBuilds:
  esbuild: true
  sharp: true
